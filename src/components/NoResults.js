import React from 'react'
import styled from 'styled-components'

const Body = styled.div`
  justify-content: center;
  margin: 25px;
  color: red;
`
const NoResults = () => <Body>Sorry, nothing found...</Body>

export default NoResults
